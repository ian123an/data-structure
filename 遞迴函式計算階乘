#include <iostream>
using namespace std;

int fact(int n)
{
	int ans;
	if(n == 1)
		return 	1;
	else
		return n * fact(n-1);
	
}

int main(int argc, char *argv[]) 
{
	int n;
	cin >> n;
	cout<<fact(n);
}
